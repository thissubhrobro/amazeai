const Transformations = () => {
  return <div>Transformations</div>;
};

export default Transformations;

// each transformation will have unique id,so need to implement dynamic routing localhost:3000/transformations/[id]
